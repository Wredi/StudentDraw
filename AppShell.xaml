<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="StudentDraw.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:pages="clr-namespace:StudentDraw.Pages"
    Shell.FlyoutBehavior="Disabled">

    <!--<ShellContent
        Title="Add/Edit"
        ContentTemplate="{DataTemplate pages:ManageListPage}"
        Route="ManageListPage" />-->
    <ShellContent
        Title="Test"
        ContentTemplate="{DataTemplate pages:SelectGradePage}"
        Route="SelectGradePage" />
    <!--<ShellContent
        Title="Test"
        ContentTemplate="{DataTemplate pages:GradeFormPage}"
        Route="GradeFormPage" />-->


</Shell>
